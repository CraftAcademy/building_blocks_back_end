.base
  %h3 Activate help request feature
  = form_for ([@building, @help_request]) do |f|
    .field
      = f.label :title
      %br/
      = f.text_field :title
    .field
      = f.label :message
      %br/
      = f.text_area :message
    .actions
      = f.submit "Send", class: 'round button'
